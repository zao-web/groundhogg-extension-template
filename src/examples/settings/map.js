/**
 * In actual usage, this would be some type of custom component, in this case, a map for settings.
 */
import { Fragment } from '@wordpress/element';

export const Map = (props) => {
	return (<Fragment />);
}